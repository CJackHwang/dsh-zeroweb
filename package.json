{
  "name": "dsh-zeroweb",
  "version": "0.0.1",
  "private": true,
  "description": "dsh (DeepSeek Harness) static browser edition — zero install, zero server, Succinix-powered execution world. Unofficial third-party port.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present || echo \"[M0] no workspace packages yet\"",
    "dev": "npm run dev --workspace=dsh-zeroweb-web",
    "lint": "npm run lint --workspaces --if-present || echo \"[M0] no workspace packages yet\"",
    "test": "npm run test --workspaces --if-present || echo \"[M0] no workspace packages yet\""
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
